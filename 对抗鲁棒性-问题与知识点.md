# 对抗鲁棒性项目：问题与知识点整理

> 记录本项目（CIFAR-10 + ResNet-18 对抗攻击与防御）开发过程中遇到的问题、原因与对应知识点。
> 按主题分组，每条 = 问题 → 知识点/解释 → 结论或修复。

---

## 一、基础概念

### 1. 训练（training） vs 推理（inference）
- **问题**：分不清"加载 checkpoint 做攻击"和"训练模型"。
- **知识点**：
  - **训练**：喂带标签数据，反复"前向 → 算损失 → 反向传播 → 优化器更新参数"，模型权重**不断被修改**。
  - **推理**：加载训练好的权重（`model.eval()` 冻结），只做前向预测，**权重不变**。
  - `.pth` 文件就是某次训练的产物（存的是学好的参数）。
- **结论**：原 notebook 的攻击单元只是推理；要加防御就得真正写训练代码。

### 2. 对抗训练（Adversarial Training）是什么
- **知识点**：在训练循环里，**先用 PGD 把每批干净图现场打成对抗样本**，再用对抗样本（标签仍为原始 `y`）训练，逼模型"被攻击也分对"。
  - 对抗样本**边训边生成**，不是提前存一批：模型每更新一次就变了，旧样本对新模型不够"狠"，所以**每个 batch 用当前模型重新打 PGD**。
  - **每轮的对抗样本数 = 这一轮遍历的训练图数**（全量则 50000），一一对应。
- **数学形式**（min-max / 鞍点问题）：
  ```
  min_θ  E[ max_{‖δ‖∞ ≤ ε}  L(f_θ(x+δ), y) ]
  ```
  外层学参数 θ 让损失变小（变鲁棒），内层在预算 ε 内找让损失最大的扰动。

---

## 二、攻击/防御方法选型

### 3. 对抗训练为什么用 PGD，而不是 DeepFool
- **问题**：能不能用 DeepFool 生成训练用的对抗样本？
- **知识点**：两者优化目标相反。
  - **PGD**：在**固定预算 ε 内最大化损失** → 给"最坏情况"样本，正好是内层 `max L` 要的。
  - **DeepFool**：求**最小 L2 扰动**、刚越过边界就停 → 样本"贴在边界上"、很弱，且每个样本扰动大小不一、没有统一预算。
- **类比**：PGD 是"在规则内全力打你"的陪练（越打越抗揍）；DeepFool 是"碰一下就收手"的陪练（练不出鲁棒性）。
- **结论**：PGD 用于训练**和**评估；DeepFool 是**评估**工具（测最小扰动大小，看鲁棒边界距离）。

### 4. AutoAttack 适合训练吗
- **问题**：训练内层用 AutoAttack 会不会更好？
- **知识点**：AutoAttack = 4 个攻击集成（APGD-CE/APGD-DLR/FAB/Square），为**评估**设计（无参数、难骗、可复现，RobustBench 标准）。
  - 不适合训练：① 太慢（上百步×多重启×黑盒查询，训练每 batch 都跑会慢上百倍）；② 含黑盒/不可微组件（Square）；③ 边际收益低。
- **结论**：**训练用 PGD，评估用 AutoAttack**（RobustBench 惯例）。

### 5. APGD 用于训练
- **知识点**：APGD 的优势（自适应步长）要 50~100 步才显现；对抗训练只能负担 7~10 步，此时 APGD≈PGD 却多背开销 → **高成本低增益**。
- **结论**：APGD 是评估的强攻击，不是训练的内层；想更强用更多 PGD 步/重启即可。

### 6. 还有哪些防御手段
- **对抗训练**（最有效，金标准）：PGD-AT / TRADES / MART。
- **输入预处理**（弱）：高斯平滑、JPEG 压缩、位深缩减——基本被自适应攻击绕过。
- **认证防御**（有保证）：Randomized Smoothing、IBP/CROWN。
- **检测**：判断"是不是对抗样本"然后拒绝。
- ⚠️ 很多"看着有效"的防御是**梯度掩蔽**造成的假象，必须用 AutoAttack 等强攻击验证。

---

## 三、评估指标

### 7. 攻击成功率怎么算 / 条件成功率
- **问题**：原代码 `success += (pred_adv != label)` 把"本来就分错"的也算成功，会被骗。
- **知识点**：
  - **无条件成功率** = 对抗样本上的错误率（含模型本来就错的）→ 不纯净。
  - **条件攻击成功率（论文口径）**：
    ```
    ASR = #(干净分对 且 对抗分错) / #(干净分对)
    ```
    只统计"模型本来分对、被攻击翻掉"的样本。
- **修复**：`evaluate_attack_optimized` 改为条件口径，并同时返回 Clean/Robust Acc。

### 8. 论文一般怎么算（Clean Acc + Robust Acc）
- **知识点**（防御类论文主流，全测试集上算）：
  - **Clean Acc** = 干净图分对的比例（模型平时准不准）。
  - **Robust Acc** = 对抗图分对的比例（被攻击后还认得对吗）。算法一样，只是测试图一个干净、一个被攻击。
  - 两者**配对报**，并用**强攻击（AutoAttack）**测 Robust Acc。
  - 关系：`Robust Acc + 无条件攻击成功率 = 100%`（注意与条件 ASR 不同）。
- **为什么配对看**：模型塌缩时 Clean/Robust **都低**，一眼看穿"不是真鲁棒"。
- **最小扰动攻击（DeepFool/C&W）**：不报成功率，报**平均/中位 L2**。

### 9. "假鲁棒"陷阱
- **现象**：塌缩模型 Clean Acc 也塌到 ~13%，于是对抗图错误率（=旧成功率）反而比正常模型低，看着"更鲁棒"。
- **结论**：必须同时看 Clean Acc，且攻击成功率用条件口径。

---

## 四、分辨率与威胁模型

### 10. 8/255 是什么
- **知识点**：L∞ 扰动预算 ε，指**每个像素最多改动 8（在 0~255 刻度上）**，换算到 [0,1] 空间 ≈ `0.0314`。
  - 小到人眼看不出，却足以骗倒无防御模型——这是对抗样本"看着一样却骗过模型"的来源。
  - CIFAR-10 的标准基准值；"L∞"指所有像素改动量的**最大值**不超过 ε。

### 11. 为什么 224 分辨率把 8/255 放大成超强攻击
- **知识点**：ε=8/255 是**逐像素** L∞ 预算（每像素预算不变），但像素越多，攻击的总扰动能量越大、自由度越高：
  | 分辨率 | 像素数 | 同样 8/255 的 L2 预算上限 |
  |---|---|---|
  | 原生 32×32 | 3,072 | ≈ 1.7 |
  | 上采样 224×224 | 150,528 | ≈ 12.2（约 7 倍） |
- **后果**：224 上同样 8/255 的攻击实际强约 7 倍，鲁棒性极难练（曾出现 adv-acc 长期为 0）；而且扰动作用在"放大后不存在的高频细节"上，威胁模型不真实。

### 12. 路线 A（224+预训练） vs 路线 B（32×32 原生）
| | 路线 A | 路线 B（已采用） |
|---|---|---|
| 为什么 | 复用 ImageNet 预训练权重（须 224 输入） | CIFAR 真实分辨率，学界标准 |
| 攻击强度(8/255) | 被放大，超强 | 标准、可防御 |
| 鲁棒性可达性 | 很难 | 可达（PGD-AT ~50%） |
| 与论文可比 | 否 | **是** |
| 代价 | 有现成 checkpoint | 改 stem + 从零训（32 很快） |

### 13. CIFAR 版 ResNet-18 的 stem 改造
- **问题**：torchvision ResNet-18 的 stem 是 `7×7 stride2 卷积 + maxpool`，会把 32×32 一上来压到 8×8，信息损失太大。
- **修复**：
  ```python
  model.conv1 = nn.Conv2d(3, 64, kernel_size=3, stride=1, padding=1, bias=False)
  model.maxpool = nn.Identity()
  ```
  改成 `3×3 stride1 + 去 maxpool`，在 32×32 上保住空间分辨率（Madry/RobustBench 标准做法）。
- **注意**：改了 stem 与 ImageNet 预训练权重不兼容，须从零在 CIFAR 上训。

### 14. 归一化（ImageNet vs CIFAR 统计量）
- **知识点**：归一化 = 逐通道 `(x-mean)/std`，让输入≈零均值单位方差，利于训练稳定/收敛。
  - ImageNet stats 与 CIFAR stats 数值很接近；**从零训用哪套几乎无差**。
  - 用 **ImageNet 预训练模型时必须用 ImageNet stats**（特征是按它学的）。
  - **唯一硬要求：训练与攻击/评估必须用同一套**（本项目 cell 0 与 cell 11 都用 ImageNet，已一致）。
- **关键**：攻击在 **[0,1] 空间**进行、`Normalize` 在模型内部，所以 **eps=8/255 的强度与用哪套 mean/std 无关**。

---

## 五、训练工程问题

### 15. 训练崩溃（loss≈ln10、acc≈随机）
- **现象**：adv-train loss=2.31（≈ln10=2.3026）、adv-acc=13.5%（≈随机 10%）→ 模型输出退化成均匀分布。
- **原因**：① LR 太高（0.01）把预训练特征冲垮；② 纯对抗损失无干净样本兜底，塌了爬不回来。
- **修复（防塌缩配方）**：
  - 小 LR（1e-3）+ **干净/对抗各半损失** `0.5·CE(clean)+0.5·CE(adv)` + **eps 预热** + **梯度裁剪** + **每轮打印 clean/adv acc** 实时监控。

### 16. adv-acc 长期为 0（但没塌缩）
- **现象**：clean-acc 稳定 ~97%、loss 在降，但 adv-acc=0%。
- **原因**：不是 bug，是 224 下 8/255 攻击太强 + 训练步数太少（5000×8/128 ≈ 320 步），打不过军备竞赛。
- **修复**：① 切到 32×32（路线 B）让 8/255 回到标准；② 全量数据 + 更多 epoch（步数 320 → ~7800）+ cosine LR。

### 17. 多 GPU（DataParallel）只用了一张卡
- **问题**：T4×2 只有一张在跑。
- **知识点**：`device="cuda"` 只指 `cuda:0`，须显式数据并行。
  - Kaggle 单进程环境用 `nn.DataParallel` 最省事（DDP 要多进程，notebook 里别扭）。
  - 只对**重计算**任务划算（训练）；攻击/评估只 200~320 样本，DataParallel 开销反而更慢。
  - 要见效须 batch 够大（喂满每张卡）。
- **checkpoint 兼容**：只包装前向用的 `train_model`，`model` 保持原始模块 → 保存的 `state_dict` 不带 `module.` 前缀。

### 18. 显存才用不到 1G
- **知识点**：
  - ResNet-18 仅约 1100 万参数（~45MB）+ CUDA context（~300-500MB），不在计算高峰时几百 MB 正常。
  - 高峰（前向存激活）只在每 batch 一瞬间，nvidia-smi 采样易错过。
  - **per-batch `empty_cache()` 会把缓存还给驱动 → 显存读数被压低**（且拖慢训练）。
  - 真实峰值用 `torch.cuda.max_memory_allocated()` 看。
  - 显存用很少 = GPU 没喂满，可加大 batch 提速。

### 19. `torch.cuda.empty_cache()` 的作用
- **知识点**：
  - PyTorch 用**缓存分配器**：张量销毁后显存不还给驱动，留在缓存池复用（避免慢速 cudaMalloc/cudaFree）。
  - `empty_cache()` = 把缓存池里**空闲**部分还给驱动；**不影响活跃张量、也不省显存**（下个 batch 又得要回来）。
  - **nvidia-smi 显示的是 reserved（保留）**，比实际用的 allocated 大。
  - **每个 batch 调是坏习惯**：拖慢训练、显存读数虚低、救不了 OOM。
  - 正确用法：偶尔在**阶段边界**调一次。

---

## 六、代码审查发现的 Bug（已修）

### 20. Bug 1 — "最佳模型"其实没保存到（浅拷贝）
- **代码**：`best_model_state = model.state_dict().copy()`
- **问题**：`state_dict()` 返回的是**活引用**，`.copy()` 只浅拷贝字典、张量仍是同一批；训练继续原地更新它们 → `best_model_state` 跟着变 → 早停装回的是**末轮**权重，不是最优。
- **修复**：`best_model_state = {k: v.detach().cpu().clone() for k, v in model.state_dict().items()}`。

### 21. Bug 2 — 训练集其实没用上数据增强
- **代码**：`random_split` 后 `val_dataset.dataset.transform = test_transform`
- **问题**：`random_split` 返回的两个 Subset **共享同一底层 dataset**，改 val 的 transform 把**训练集的增广也一并去掉了**（等于无增广训练）。
- **修复**：给验证集**单独建一个 `datasets.CIFAR10` 实例**（套 test_transform），按同一套随机索引切 `Subset`，训练子集用增广实例、验证子集用无增广实例。

### 22. 其他一致性/规范问题（已修）
- **eps 不统一**：FGSM 原用 `0.03`，APGD/训练用 `8/255` → 统一为 `8/255`。
- **过期注释**：cell 16 标题"子集微调"→"全量微调"、"预训练 model"措辞；cell 13/14 删掉已不存在的 `res_fgsm`/`res_df` 引用。
- **术语冲突**：混淆矩阵标题 `attack success` → `adv error`（与"条件攻击成功率"区分）。
- **残留清理**：删掉若干 per-batch `empty_cache()`/`gc.collect()`。
- **CKPT 路径**：cell 4/11 指向本地 `resnet18_cifar10_best.pth`（匹配新 32×32 结构，避免误加载旧 224 权重）。

---

## 七、其他要点

### 23. 对抗训练是微调还是从零
- 本项目是**在干净模型上微调**（`robust_net = copy.deepcopy(model)`，model 已加载干净权重），不是随机初始化从零训。
- 微调初始化收敛快、clean acc 保得好；但**仍需足够轮数**——现用 SGD lr=0.1 + MultiStepLR 训 **80 轮**（早期只训 20 轮属欠训练，详见 §26–28）。从随机初始化从零对抗训练需更多轮、成本翻几倍，对本项目无必要。

### 24. 鲁棒模型的保存
- cell 16 结尾 `torch.save({'model_state_dict': robust_net.state_dict()}, "resnet18_cifar10_pgd_at.pth")`，与基础模型文件区分。
- `robust_net` 是原始模块 → 不带 `module.` 前缀，可直接加载进 CIFAR-stem 版模型。

### 25. 矢量图保存
- 所有图保存为 **PDF（矢量）**：散点/折线/文字无限放大不糊；imshow 类（对抗样本图、混淆矩阵）作为内嵌位图（照片无法矢量化，正常）。
- 全局 `savefig.dpi=300` 控制内嵌位图清晰度。

---

## 八、本轮改进（欠训练修复 + 评估增强 + 工程）

### 26. 诊断：对抗训练欠训练（robust acc 偏低）
- **现象**：20 epoch 训完，APGD(强攻击) robust acc 仅 ~17%（标准 PGD-AT 应 ~45–50%）；日志显示末轮 `lr` 已衰到 `1e-5`、而 adv-acc 才 ~19% 仍在上升 → 没收敛就停了。
- **原因**：epoch 太少 + 调度让 LR 过早衰减到极小，鲁棒特征还没训出来。
- **修复**：升级训练配方（见 §27）。

### 27. 当前对抗训练方案（cell 16）
- **骨架 = 标准 Madry PGD-AT**：
  - `SGD(lr=0.1, momentum=0.9, weight_decay=5e-4)`
  - `MultiStepLR`，在 **40 / 60 轮（50% / 75%）处 ÷10**，共 **80 轮**（取代原 20 轮 + CosineAnnealing）
  - 内层 **PGD-7**（eps=8/255、随机起点、`α=2.5·eps/steps`、每步 Linf 投影 + clamp 回 [0,1]）
- **三层防塌缩补丁**（沿用 §15 的经验）：
  - 干净/对抗各半损失 `0.5·CE(clean)+0.5·CE(adv)`
  - eps 预热（前 5 轮从 0.5×eps 线性升到 1×eps）
  - 梯度裁剪（`clip_grad_norm_=1.0`）
- **从干净模型微调**（非从零），省时间、保 clean acc。
- **每轮记录** clean/adv acc + loss → 训练后输出训练双曲线矢量图（见 §31）。

### 28. 为什么采用当前方案（每个选择的动机）
| 选择 | 动机 |
|---|---|
| 干净+对抗各半损失 | 上一版纯对抗损失塌缩过（loss≈ln10），干净项当锚防"躺平" |
| 从干净模型微调 | Kaggle 单次时长有限 + 已有干净模型 → 收敛快又稳 |
| eps 预热 | 一上来就满 8/255 会把模型打懵、早期塌缩 |
| 梯度裁剪 | LR=0.1 偶发梯度尖峰的安全带 |
| PGD-7（非 10/20） | 每 batch 前向 ~9 次（内层 7 + clean + adv），是普通训练 ~10 倍；7 步是"鲁棒性 vs 耗时"折中 |
| [0,1] 空间攻击 | 训练/攻击/评估口径一致 → robust acc 诚实，避免假鲁棒 |
| 单卡（非 DataParallel） | 小模型多次前向，DP 反复复制 + scatter/gather 产生碎片化 OOM 且几乎不提速 |
- **一句话**：标准 PGD-AT 骨架 + 三层防塌缩补丁 + 诚实评估口径，再按 T4 算力砍到 PGD-7 / 单卡 / 微调。每个"非标准"选择都对应一个踩过的具体问题。

### 29. 评估加一档 PGD-20（攻击强度阶梯）
- **动机**：原来只有 FGSM(弱) 和 APGD(强)，中间缺学术界标准基准。
- **阶梯**：**FGSM(单步, 弱) < PGD-20(20 步, 标准) < APGD(自适应, 最强)**。
  - `torchattacks.PGD(model, eps=8/255, alpha=2/255, steps=20, random_start=True)`
  - 同一模型三者 robust acc 应**依次递减**；若不递减（如 PGD-20 比 FGSM 还高）→ 评估有 bug（典型是梯度混淆）。
  - PGD-20 是论文事实标准，便于和文献中的 robust acc 数值**直接对比**。

### 30. Robust Acc 与攻击的关系（概念澄清）
- **Robust Acc** = 模型在"被该攻击打过的对抗样本"上的准确率；攻击越强，数值越低。
- 普通模型 Clean 90% → FGSM 1.88% → APGD 0%：被打穿，正是**无梯度混淆**的诚实信号。
- 鲁棒模型 Clean 73% → FGSM 25.6% → APGD 17%：robust acc 在三档攻击下都高于普通模型 = 防御真有效。
- **报鲁棒性以最强攻击（APGD/AutoAttack）为准**，FGSM 会高估。

### 31. 矢量图：分开保存 + 自动建目录（cell 11 / 16 / 17）
- `tsne_facet_by_class` 加 `save_each=True`：Clean / FGSM 在**共享同一套 t-SNE 坐标**下各存一张独立矢量图（坐标系一致、可直接叠放对比），文件名按 `save_path` 派生（`..._Clean.pdf` / `..._FGSM.pdf`）。
- `save_path` 自动 `os.makedirs(...)`：目标子目录不存在会创建（修复了 `to/dest/...` 这类路径直接 savefig 报错）。
- 新增两张矢量图（同时存 **SVG + PDF**）：cell 16 训练双曲线 `pgd_at_training_curves`、cell 17 鲁棒性对比柱状图 `robustness_comparison`（图内文字用英文，避免 Kaggle 缺中文字体显示方块）。

### 32. Kaggle 路径与跳过干净训练
- 干净模型放 Kaggle input（**只读**）：`/kaggle/input/notebooks/liangliguo/cifar/resnet18_cifar10_best.pth`。
- **cell 4** 设 `SKIP_TRAINING=True` + 该路径 → 跳过干净训练直接加载；**cell 11** 用同一路径加载作攻击基线（两处加载点已对齐）。
- 所有产物（鲁棒模型 `resnet18_cifar10_pgd_at.pth`、矢量图）写工作目录 `/kaggle/working/`（input 不可写）。

---

## 附：正确的实验流程（路线 B）

1. **cell 0–8**：原生 32×32 从零训干净 ResNet-18（改 stem），存 `resnet18_cifar10_best.pth`（T4×2 约 15–30 分钟，clean ~93–95%）。
2. **cell 11**：加载该权重，构建 `atk_model`（内含归一化）。
3. **cell 12–15**：FGSM/DeepFool/APGD 攻击 + t-SNE + 混淆矩阵。
4. **cell 16**：全量 PGD 对抗训练（标准 SGD lr=0.1 + MultiStepLR 80 轮 + 防塌缩配方，详见 §26–28），存鲁棒模型。
5. **cell 17**：诚实评估——FGSM（弱）+ PGD-20（标准）+ APGD（强）三档对比"普通 vs 鲁棒"的 Clean/Robust Acc。

**核心准则**：训练用 PGD、评估用强攻击；Clean 与 Robust Acc 配对看；攻击在 [0,1] 空间、归一化进模型；eps 全程同口径（8/255）。
