% !TEX program = xelatex
% ============================================================
%  对抗攻击与对抗训练 实验报告 —— Beamer 文稿
%  主线：① 训练模型  ② DeepFool 攻击（核心）  ③ 对抗训练（防御）
%  编译方式：xelatex at.tex （需运行两次以生成目录/导航）
%
%  本文件使用 fontspec + Noto CJK 字体，无需 ctex 宏包，通用性强。
%  如果你的环境装了 ctex，也可以把下面前导区换成：
%      \documentclass[aspectratio=169]{ctexbeamer}
%  并删去 fontspec / XeTeXlinebreaklocale 相关四行即可。
% ============================================================
\documentclass[aspectratio=169,11pt]{beamer}

% ---------- 中文支持（fontspec 方案，XeLaTeX 编译） ----------
\usepackage{fontspec}
\setmainfont{Noto Serif CJK SC}
\setsansfont{Noto Sans CJK SC}
\XeTeXlinebreaklocale "zh"
\XeTeXlinebreakskip=0pt plus 1pt minus 0.1pt

% ---------- 数学与图表宏包 ----------
\usepackage{amsmath,amssymb,mathtools}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,calc,positioning}

% ---------- 主题 ----------
\usetheme{Madrid}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{caption}[numbered]

% ---------- 数学算子 ----------
\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareMathOperator*{\argmax}{arg\,max}
\DeclareMathOperator{\sign}{sign}
\newcommand{\norm}[1]{\left\lVert #1 \right\rVert}
\newcommand{\bx}{\boldsymbol{x}}
\newcommand{\br}{\boldsymbol{r}}
\newcommand{\bw}{\boldsymbol{w}}
\newcommand{\bdelta}{\boldsymbol{\delta}}
% 数据待补占位符
\newcommand{\TBD}{\textcolor{red}{\rule{1.6em}{0.6em}}}

% ---------- 标题信息 ----------
\title[对抗攻击与对抗训练]{对抗攻击与对抗训练实验报告}
\subtitle{以 DeepFool 为核心：模型训练 · 攻击 · 防御}
\author[王军 \and 梁利国 \and 汤付伟]{王军 \quad 梁利国 \quad 汤付伟 \\[0.6em]
  {\scriptsize\itshape\color{gray} Co-authored with Claude Opus 4.8}}
\date{\today}

% ---------- 每章前插入目录页 ----------
\AtBeginSection[]{
  \begin{frame}{本章内容}
    \tableofcontents[currentsection]
  \end{frame}
}

\begin{document}

% ============ 封面 ============
\begin{frame}
  \titlepage
\end{frame}

% ============ 目录 ============
\begin{frame}{目录}
  \tableofcontents
\end{frame}

% ================================================================
\section{背景与实验目标}
% ================================================================

\begin{frame}{什么是对抗样本？}
  \begin{block}{核心现象}
    给一张能被模型正确分类的图像，叠加一个\textbf{人眼几乎察觉不到}的微小扰动，
    模型就会以高置信度把它分错。这个被扰动后的图像称为\textbf{对抗样本}。
  \end{block}
  \vspace{0.5em}
  \begin{itemize}
    \item 扰动 $\br$ 很小：$\norm{\br}$ 接近 0，视觉上与原图无差别。
    \item 但模型输出 $\hat k(\bx+\br) \neq \hat k(\bx)$。
    \item 说明深度网络的决策边界在高维空间里非常“贴近”正常样本。
  \end{itemize}
  \vspace{0.5em}
  \alert{研究意义：} 评估模型鲁棒性、理解决策边界、改进训练（对抗训练）。
\end{frame}

\begin{frame}{本次实验的三条主线}
  \begin{columns}[T]
    \begin{column}{0.33\textwidth}
      \begin{block}{① 训练模型}
        \footnotesize
        在 CIFAR-10 上微调 ResNet-18，得到一个准确的\textbf{基线模型}，作为被攻击对象。
      \end{block}
    \end{column}
    \begin{column}{0.33\textwidth}
      \begin{block}{② DeepFool 攻击}
        \footnotesize
        \textbf{（核心）}用最小扰动把基线模型骗倒，并与 FGSM / APGD 对比。
      \end{block}
    \end{column}
    \begin{column}{0.33\textwidth}
      \begin{block}{③ 对抗训练}
        \footnotesize
        用 PGD 对抗训练得到\textbf{鲁棒模型}，诚实评估防御是否真有效。
      \end{block}
    \end{column}
  \end{columns}
  \vspace{0.8em}
  \begin{center}
  \begin{tikzpicture}[>=Stealth, node distance=8mm]
    \node[draw,rounded corners,fill=blue!8,minimum height=7mm] (a) {训练基线模型};
    \node[draw,rounded corners,fill=green!8,minimum height=7mm,right=14mm of a] (b) {DeepFool 攻击};
    \node[draw,rounded corners,fill=orange!12,minimum height=7mm,right=14mm of b] (c) {对抗训练 + 评估};
    \draw[->,thick] (a) -- (b);
    \draw[->,thick] (b) -- (c);
  \end{tikzpicture}
  \end{center}
  \footnotesize 攻击是“矛”，对抗训练是“盾”；先有准确的模型，才谈得上攻击与防御。
\end{frame}

% ================================================================
\section{模型训练}
% ================================================================

\begin{frame}{数据集与预处理}
  \begin{columns}[T]
    \begin{column}{0.55\textwidth}
      \textbf{CIFAR-10}：10 类自然图像，原生 $32\times32$。
      \begin{itemize}
        \item 训练 / 验证 / 测试 = \textbf{45000 / 5000 / 10000}
        \item 数据增广：\texttt{RandomCrop(32, padding=4)} + \texttt{RandomHorizontalFlip}
        \item 归一化：均值 $[0.485,0.456,0.406]$，标准差 $[0.229,0.224,0.225]$
        \item 攻击阶段图像保持在 $[0,1]$ 像素空间，\textbf{归一化包进模型}内部
              （避免攻击库的 \texttt{clamp(0,1)} 破坏对抗样本）。
      \end{itemize}
    \end{column}
    \begin{column}{0.45\textwidth}
      \begin{block}{为什么把归一化放进模型}
        \footnotesize
        攻击需要在合法像素区间 $[0,1]$ 内裁剪。若先归一化再攻击，
        \texttt{clamp(0,1)} 会把归一化后的负值截断、毁掉图像。
        统一约定：\textbf{数据 $[0,1]$，归一化作为模型第一层}。
      \end{block}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{模型结构：CIFAR 版 ResNet-18}
  直接用 ImageNet 版 ResNet-18 会把 $32\times32$ 一上来就压到 $8\times8$，
  丢失太多空间信息。需做两处\textbf{适配小图}的改造：
  \begin{itemize}
    \item \texttt{conv1}：$7\times7$ stride 2 $\;\to\;$ \textbf{$3\times3$ stride 1, padding 1}
    \item \texttt{maxpool}：$\;\to\;$ \textbf{Identity（去掉）}
    \item \texttt{fc}：输出维度改为 \textbf{10 类}
  \end{itemize}
  \vspace{0.4em}
  \begin{itemize}
    \item 参数量 $\approx 11.2$\,M，从零开始训练（\texttt{weights=None}）。
    \item 固定随机种子 \texttt{seed=42}，保证可复现。
  \end{itemize}
\end{frame}

\begin{frame}{训练配置与结果}
  \begin{columns}[T]
    \begin{column}{0.5\textwidth}
      \textbf{训练配置}
      \begin{itemize}
        \item 损失：交叉熵
        \item 优化器：\texttt{Adam}，lr $=10^{-3}$，weight decay $=10^{-4}$
        \item 学习率调度：\texttt{StepLR}（每 5 轮 $\times 0.1$）
        \item 早停：验证准确率连续 \textbf{5} 轮不提升即停
        \item 最多 30 轮（早停自动截断）
      \end{itemize}
    \end{column}
    \begin{column}{0.5\textwidth}
      \textbf{结果}（实际训练 \textbf{19} 轮，早停于第 17 轮取最佳）
      \begin{table}
      \centering\footnotesize
      \begin{tabular}{lc}
        \toprule
        指标 & 数值 \\
        \midrule
        最佳验证准确率 & 88.96\% \\
        测试 Top-1 准确率 & \alert{88.53\%} \\
        测试 Macro-F1 & 88.50\% \\
        \bottomrule
      \end{tabular}
      \end{table}
      \vspace{0.4em}
      \footnotesize 该模型作为后续所有攻击 / 防御实验的\textbf{基线（普通模型）}，
      权重存于 \texttt{resnet18\_cifar10\_best.pth}。
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{训练曲线（Loss / Accuracy）}
  \begin{center}
    \includegraphics[width=0.92\textwidth]{training_curves.pdf}
  \end{center}
  \footnotesize
  约第 6 轮（\texttt{StepLR} 首次衰减）后曲线明显跃升并迅速收敛；
  验证准确率在第 17 轮达到峰值 \textbf{88.96\%}，此后连续 5 轮无提升触发早停（虚线处为最佳轮）。
\end{frame}

\begin{frame}{基线模型：干净测试集混淆矩阵}
  \begin{columns}[c]
    \begin{column}{0.6\textwidth}
      \includegraphics[width=\linewidth]{confusion_clean.pdf}
    \end{column}
    \begin{column}{0.4\textwidth}
      \footnotesize
      行归一化（每行 = 该真实类别被预测到各类的比例）。
      \begin{itemize}
        \item 对角线普遍 \textbf{0.8--0.95}，整体准确率 \textbf{88.53\%}。
        \item 最易混的是 \textbf{cat\,$\leftrightarrow$\,dog}（cat 仅 0.74，最低）。
        \item \texttt{automobile}/\texttt{ship}/\texttt{truck} 最稳（$\ge 0.93$）。
        \item 误分主要发生在\textbf{语义相近}的类别之间，符合直觉。
      \end{itemize}
    \end{column}
  \end{columns}
\end{frame}

% ================================================================
\section{DeepFool 攻击方法（核心）}
% ================================================================

\begin{frame}{对抗攻击的形式化定义}
  对于分类器 $f$ 与样本 $\bx$，记其预测标签
  \[
    \hat k(\bx) = \argmax_k f_k(\bx).
  \]
  寻找“最小”对抗扰动可写成优化问题：
  \[
    \Delta(\bx;f) \;:=\; \min_{\br}\; \norm{\br}_2
    \quad \text{s.t.}\quad \hat k(\bx+\br) \neq \hat k(\bx).
  \]
  \begin{itemize}
    \item $\Delta(\bx;f)$ 度量了样本 $\bx$ 到\textbf{决策边界}的距离。
    \item 这个距离越小，说明模型在该点越“脆弱”。
    \item \textbf{DeepFool 的目标：} 高效、准确地求解这个最小扰动。
  \end{itemize}
\end{frame}

\begin{frame}{回顾 FGSM 及其局限}
  最经典的攻击 FGSM（Fast Gradient Sign Method）只走一步：
  \[
    \br = \varepsilon \cdot \sign\!\big(\nabla_{\bx} J(\theta,\bx,y)\big).
  \]
  \begin{itemize}
    \item \textbf{优点：} 单步、极快，直觉清晰（沿损失上升最快方向推一步）。
    \item \textbf{局限一：} 取 $\sign$ 把每个像素都顶到 $\pm\varepsilon$，扰动\textbf{粗糙且偏大}。
    \item \textbf{局限二：} 不是为“最小扰动”设计的，$\varepsilon$ 需要\textbf{手动调}。
  \end{itemize}
  \vspace{0.4em}
  \begin{exampleblock}{由此引出 DeepFool}
    能不能不手调参数，自动找到\textbf{刚好越过决策边界}的最小扰动？
  \end{exampleblock}
\end{frame}

\begin{frame}{几何直觉：决策边界就是“超平面”}
  \begin{itemize}
    \item 在样本附近，把分类器\textbf{局部线性化}，决策边界近似为一个个\textbf{超平面}。
    \item 让样本改变类别，最省力的做法是：沿\textbf{垂直方向}推到\textbf{最近}的那个边界外。
    \item 这正是“点到平面的最短距离”——线性代数里的经典问题。
  \end{itemize}
  \vspace{0.3em}
  \begin{center}
  \begin{tikzpicture}[>=Stealth, scale=0.95]
    % 最近边界（较陡的线，离 x 近）
    \draw[thick,gray] (1.7,-1.6) -- (2.1,1.8);
    % 较远边界（较平的线，离 x 远）
    \draw[thick,gray] (-1.8,2.7) -- (3.8,3.1);
    % 样本点 x
    \fill (0,0) circle (2.5pt);
    \node[below left] at (0,0) {$\bx$};
    % 选中的最小扰动：指向最近边界
    \draw[->,very thick,orange!85!red] (0,0) -- (1.97,0.03)
      node[midway,above] {\small $\br_*$（最小）};
    % 被舍弃的更远方向
    \draw[->,thick,gray,dashed] (0,0) -- (0,2.92)
      node[midway,right] {\small 更远 → 舍弃};
    % 区域标注
    \node at (-1.25,-1.1) {\small class A（当前）};
    \node at (3.0,-0.7) {\small class B};
    \node at (-1.1,3.4) {\small class C};
  \end{tikzpicture}
  \end{center}
  \footnotesize 在所有相邻类别的边界中，DeepFool 选择\textbf{最近}的那条，做垂直投影。
\end{frame}

\begin{frame}{二分类：线性分类器（最简单情形）}
  设二分类器 $f(\bx)=\bw^\top\bx + b$，预测 $\hat k(\bx)=\sign\!\big(f(\bx)\big)$。
  \vspace{0.4em}

  样本 $\bx$ 到超平面 $\{\,\bx: f(\bx)=0\,\}$ 的最小扰动，就是\textbf{正交投影}：
  \[
    \boxed{\;\br_*(\bx) = -\,\frac{f(\bx)}{\norm{\bw}_2^{2}}\,\bw\;}
  \]
  \begin{itemize}
    \item 方向：沿法向量 $\bw$（边界的垂直方向）。
    \item 大小：$\norm{\br_*}_2 = \dfrac{|f(\bx)|}{\norm{\bw}_2}$，正是\textbf{点到平面的距离}。
    \item 这是闭式解，一步到位。
  \end{itemize}
\end{frame}

\begin{frame}{二分类：一般（非线性）分类器}
  真实网络是非线性的，没有全局超平面。DeepFool 的做法：\textbf{反复线性化 + 迭代}。
  \vspace{0.4em}

  在当前迭代点 $\bx_i$ 把 $f$ 一阶展开：
  \[
    f(\bx_i + \br) \approx f(\bx_i) + \nabla f(\bx_i)^\top \br .
  \]
  令其等于 0，得到这一步的最小扰动：
  \[
    \br_i = -\,\frac{f(\bx_i)}{\norm{\nabla f(\bx_i)}_2^{2}}\,\nabla f(\bx_i),
    \qquad \bx_{i+1} = \bx_i + \br_i .
  \]
  \begin{itemize}
    \item 每走一步都重新计算梯度（重新线性化），逼近真实弯曲的边界。
    \item 直到 $\hat k(\bx_i)$ 改变为止，通常只需 \textbf{2--4 步}。
  \end{itemize}
\end{frame}

\begin{frame}{多分类：在所有边界中选最近的一条}
  对 $c$ 类分类器 $f:\mathbb{R}^n\to\mathbb{R}^c$，记原始标签 $\hat k(\bx_0)$。
  在迭代点 $\bx_i$，对每个其他类别 $k \neq \hat k(\bx_0)$ 定义：
  \[
    \bw'_k = \nabla f_k(\bx_i) - \nabla f_{\hat k(\bx_0)}(\bx_i),
    \qquad
    f'_k = f_k(\bx_i) - f_{\hat k(\bx_0)}(\bx_i).
  \]
  \begin{itemize}
    \item $\bw'_k$：类别 $k$ 与当前类之间“等分边界”的法向。
    \item $f'_k$：当前点相对该边界的“带符号距离量”。
  \end{itemize}
  \vspace{0.3em}
  选出\textbf{最近}的边界：
  \[
    \hat l = \argmin_{k \neq \hat k(\bx_0)} \frac{|f'_k|}{\norm{\bw'_k}_2}.
  \]
\end{frame}

\begin{frame}{多分类：最小扰动公式}
  朝最近边界 $\hat l$ 做正交投影，得到本步扰动：
  \[
    \boxed{\;
      \br_i = \frac{|f'_{\hat l}|}{\norm{\bw'_{\hat l}}_2^{2}}\,\bw'_{\hat l}
    \;}
  \]
  然后更新 $\bx_{i+1} = \bx_i + \br_i$，重复直到类别改变。
  \vspace{0.6em}

  最终累计扰动并做\textbf{过冲}（overshoot），确保确实越过边界：
  \[
    \hat\br = (1+\eta)\sum_i \br_i,
    \qquad \eta = 0.02 .
  \]
  \footnotesize 过冲系数 $1.02$ 让扰动“多推一点点”，避免样本卡在边界上、因数值/量化又被推回正确类。
\end{frame}

\begin{frame}{算法伪代码（多分类 DeepFool）}
  \begin{block}{}
  \footnotesize
  \begin{enumerate}
    \item 初始化 $\bx_0 \gets \bx$，$\ i \gets 0$
    \item \textbf{while} $\hat k(\bx_i) = \hat k(\bx_0)$ \textbf{do}
    \item \quad \textbf{for} $k \neq \hat k(\bx_0)$:
          \ $\bw'_k \gets \nabla f_k(\bx_i) - \nabla f_{\hat k(\bx_0)}(\bx_i)$,
          \ $f'_k \gets f_k(\bx_i) - f_{\hat k(\bx_0)}(\bx_i)$
    \item \quad 选最近边界 \ $\hat l \gets \argmin\limits_{k \neq \hat k(\bx_0)} \dfrac{|f'_k|}{\norm{\bw'_k}_2}$
    \item \quad $\br_i \gets \dfrac{|f'_{\hat l}|}{\norm{\bw'_{\hat l}}_2^{2}}\,\bw'_{\hat l}$,
          \ \ $\bx_{i+1} \gets \bx_i + \br_i$,
          \ \ $i \gets i+1$
    \item \textbf{end while}
    \item \textbf{return} \ $\hat\br = (1+\eta)\sum_i \br_i$
  \end{enumerate}
  \end{block}
  \footnotesize 复杂度主要来自每步对 $c$ 个类别求梯度；CIFAR-10（$c=10$）规模下很快。
  本实验用 \texttt{torchattacks.DeepFool(steps=20, overshoot=0.02)}。
\end{frame}

% ================================================================
\section{攻击实验与结果}
% ================================================================

\begin{frame}{鲁棒性度量与实验设置}
  最小扰动攻击不看“成功率”，而看\textbf{平均/中位 $L_2$ 扰动}——越小越说明模型脆弱。
  论文用\textbf{平均相对扰动}：
  \[
    \hat\rho_{adv}(f) = \frac{1}{|\mathcal{D}|}
      \sum_{\bx \in \mathcal{D}} \frac{\norm{\hat\br(\bx)}_2}{\norm{\bx}_2}.
  \]
  \begin{itemize}
    \item \textbf{被攻击模型：} 上一节训好的 ResNet-18（干净准确率 88.53\%）。
    \item \textbf{攻击实现：} \texttt{torchattacks}，全部在 $[0,1]$ 像素空间，取 200 个测试样本。
    \item \textbf{对照攻击：} FGSM、APGD（均 $L_\infty$，$\varepsilon=8/255$）。
    \item \textbf{成功率口径：} \textbf{条件攻击成功率} = 干净分对、却被攻击翻掉的比例。
  \end{itemize}
\end{frame}

\begin{frame}{实测结果：三种攻击对比（普通模型，白盒）}
  \begin{table}
  \centering
  \footnotesize
  \begin{tabular}{lcccc}
    \toprule
    攻击 & 范数 / 预算 & 攻击成功率 & Robust Acc & 平均 $L_2$ \\
    \midrule
    FGSM     & $L_\infty,\ 8/255$ & 97.91\% & 1.88\% & 1.7208 \\
    \alert{DeepFool} & \alert{$L_2$（最小扰动）} & \alert{97.21\%} & \alert{2.50\%} & \alert{\textbf{0.1604}} \\
    APGD     & $L_\infty,\ 8/255$ & 100.00\% & 0.00\% & 1.3048 \\
    \bottomrule
  \end{tabular}
  \end{table}
  \vspace{0.5em}
  \begin{itemize}
    \item DeepFool 的平均 $L_2$ 仅 \textbf{0.16}，约为 FGSM（1.72）的 \alert{$1/11$}、APGD（1.30）的 $1/8$。
    \item 三者骗过率都很高，但 DeepFool 用\textbf{小一个量级}的扰动达成同样效果。
    \item 干净准确率均为 89.69\%（200 样本子集口径）。
  \end{itemize}
\end{frame}

\begin{frame}{实测结果：扰动大小分布（FGSM vs DeepFool）}
  \begin{center}
    \includegraphics[height=0.66\textheight]{l2_distribution.pdf}
  \end{center}
  \footnotesize
  DeepFool 的 $L_2$ 扰动集中在接近 0 的位置，整体分布远在 FGSM 左侧——
  这正是“最小扰动”路线的直接证据。
\end{frame}

\begin{frame}{实测结果：混淆矩阵（对抗样本上）}
  \begin{columns}[T]
    \begin{column}{0.5\textwidth}
      \centering FGSM\\[0.3em]
      \includegraphics[width=\linewidth]{confusion_fgsm.pdf}
    \end{column}
    \begin{column}{0.5\textwidth}
      \centering DeepFool\\[0.3em]
      \includegraphics[width=\linewidth]{confusion_deepfool.pdf}
    \end{column}
  \end{columns}
  \vspace{0.3em}
  \footnotesize 对角线（仍分类正确）几乎清空：两种攻击都把样本大量推向\textbf{相邻类别}，
  但 DeepFool 用的扰动小得多。
\end{frame}

\begin{frame}{实测结果：特征空间 t-SNE（Clean vs 对抗）}
  \begin{center}
    \includegraphics[height=0.66\textheight]{tsne_clean_vs_adv.pdf}
  \end{center}
  \footnotesize
  共享嵌入下，干净样本按类别聚成清晰簇；对抗样本（FGSM/DeepFool/APGD）则被打散、
  跨越到其他类别的区域，直观展示决策被改写。
\end{frame}

% ================================================================
\section{对抗训练（防御）}
% ================================================================

\begin{frame}{对抗训练：把“最坏样本”喂给模型}
  防御思路：训练时\textbf{先用 PGD 把每批干净图现场打成对抗样本}，再用它（标签仍为原始 $y$）训练，
  逼模型“被攻击也分对”。形式化为\textbf{min-max（鞍点）问题}：
  \[
    \min_{\theta}\ \mathbb{E}_{(\bx,y)}\Big[\ \max_{\norm{\bdelta}_\infty \le \varepsilon}\ L\big(f_\theta(\bx+\bdelta),\,y\big)\Big].
  \]
  \begin{itemize}
    \item \textbf{内层 $\max$：} 在预算 $\varepsilon$ 内找让损失最大的扰动（用 PGD 近似）。
    \item \textbf{外层 $\min$：} 更新参数 $\theta$，让“最坏情况”的损失也变小 $\Rightarrow$ 变鲁棒。
    \item 对抗样本\textbf{边训边生成}：模型每更新一次就变，需用当前模型重新打 PGD。
  \end{itemize}
\end{frame}

\begin{frame}{为什么用 PGD，而不是 DeepFool？}
  \begin{columns}[T]
    \begin{column}{0.5\textwidth}
      \begin{block}{PGD（用于训练）}
        \footnotesize
        在\textbf{固定预算 $\varepsilon$ 内最大化损失} → 给“最坏情况”样本，
        正好是内层 $\max L$ 要的。\\[0.3em]
        \emph{像“在规则内全力打你”的陪练 → 越打越抗揍。}
      \end{block}
    \end{column}
    \begin{column}{0.5\textwidth}
      \begin{block}{DeepFool（不用于训练）}
        \footnotesize
        求\textbf{最小扰动、刚越界就停} → 样本“贴在边界上”、很弱，
        且每个样本扰动大小不一、\textbf{没有统一预算}。\\[0.3em]
        \emph{像“碰一下就收手”的陪练 → 练不出鲁棒性。}
      \end{block}
    \end{column}
  \end{columns}
  \vspace{0.5em}
  \begin{exampleblock}{结论}
    \footnotesize
    \textbf{PGD} 同时用于训练与评估；\textbf{DeepFool} 是\textbf{评估}工具
    （测最小扰动大小、看到决策边界的距离）。
  \end{exampleblock}
\end{frame}

\begin{frame}{PGD 对抗训练配方（本实验）}
  \begin{columns}[T]
    \begin{column}{0.55\textwidth}
      \footnotesize
      \begin{itemize}
        \item 训练集：全量 50000，原生 $32\times32$ + 增广
        \item 内层攻击：\textbf{PGD-7}，$L_\infty$，$\varepsilon = 8/255$
        \item $\varepsilon$ 预热：前 5 轮从 $0.5\varepsilon$ 线性升到 $\varepsilon$
        \item 损失：$(1-\lambda)\,\mathrm{CE}_{\text{clean}} + \lambda\,\mathrm{CE}_{\text{adv}}$，$\lambda=0.5$
              （干净样本兜底\textbf{防塌缩}）
        \item 优化器：\texttt{SGD}（lr $=0.1$, mom $=0.9$, wd $=5\times10^{-4}$）
        \item 调度：\texttt{MultiStepLR} 在 40 / 60 轮 $\div 10$，共 \textbf{80} 轮
        \item 梯度裁剪：上限 $1.0$
      \end{itemize}
    \end{column}
    \begin{column}{0.45\textwidth}
      \begin{block}{防“假鲁棒”要点}
        \footnotesize
        \begin{itemize}
          \item 干净/对抗损失\textbf{各半}，避免模型塌缩（Clean/Robust 双低）。
          \item 评估用\textbf{强攻击}（APGD），并按强度阶梯 FGSM < PGD-20 < APGD 排查梯度混淆。
          \item Clean 与 Robust Acc \textbf{配对报告}。
        \end{itemize}
      \end{block}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{防御效果：普通模型 vs 鲁棒模型}
  攻击强度阶梯：FGSM（弱）< PGD-20（标准基准）< APGD（最强）。同一模型 Robust Acc 应\textbf{依次递减}。
  \begin{table}
  \centering
  \footnotesize
  \begin{tabular}{lcccc}
    \toprule
     & Clean Acc & FGSM & PGD-20 & APGD \\
    \midrule
    普通模型 & 89.69\% & 1.88\% & \TBD & 0.00\% \\
    鲁棒模型 & \TBD & \TBD & \TBD & \TBD \\
    \bottomrule
  \end{tabular}
  \end{table}
  \vspace{0.4em}
  \footnotesize
  \textbf{读表方法：} 对抗训练若真有效，鲁棒模型在三种攻击下的 Robust Acc 都应\textbf{明显高于}普通模型；
  代价是 Clean Acc 通常会下降（鲁棒性—准确率权衡）。
  \\[0.3em]
  \textcolor{red}{\TBD\ 处为最终 80 轮训练结果，待训练完成后填入。}
\end{frame}

% ================================================================
\section{讨论与总结}
% ================================================================

\begin{frame}{DeepFool 的优点与局限}
  \begin{columns}[T]
    \begin{column}{0.5\textwidth}
      \begin{block}{优点}
        \begin{itemize}
          \item 扰动\textbf{最小}、几乎不可见
          \item \textbf{无需手调}超参数
          \item 迭代少、\textbf{速度快}
          \item 几何直觉清晰、\textbf{易讲解}
          \item 提供可比的鲁棒性度量
        \end{itemize}
      \end{block}
    \end{column}
    \begin{column}{0.5\textwidth}
      \begin{block}{局限}
        \begin{itemize}
          \item 默认\textbf{无定向}（不能指定目标类）
          \item 扰动卡在边界、\textbf{较“脆”}（易被量化/压缩抵消，靠过冲缓解）
          \item 纯攻击\textbf{强度}略逊于 C\&W
          \item \textbf{不适合}用于对抗训练
        \end{itemize}
      \end{block}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{与其他攻击方法的关系}
  \begin{table}
  \centering
  \footnotesize
  \begin{tabular}{lll}
    \toprule
    方法 & 思路 & 特点 \\
    \midrule
    FGSM     & 沿梯度符号走一步     & 单步、$L_\infty$、扰动大 \\
    PGD      & 在 $\varepsilon$ 球内迭代最大化损失 & 迭代、$L_\infty$、训练/评估都用 \\
    APGD     & 自适应步长 + 重启     & 强评估基线（AutoAttack 组件） \\
    \alert{DeepFool} & \alert{投影到最近决策边界} & \alert{迭代、最小扰动、快、几何} \\
    C\&W     & 优化最小 $L_2$ 扰动   & 最强、扰动极小、较慢 \\
    \bottomrule
  \end{tabular}
  \end{table}
  \vspace{0.5em}
  \begin{itemize}
    \item DeepFool 与 C\&W 同属“\textbf{最小扰动}”路线（评估到边界的距离）。
    \item FGSM / PGD / APGD 属“\textbf{固定预算}”路线（关注给定 $\varepsilon$ 内的破坏力）。
  \end{itemize}
\end{frame}

\begin{frame}{总结}
  \begin{enumerate}
    \item \textbf{训练模型：} CIFAR 版 ResNet-18（conv1 改 $3\times3$、去 maxpool），
          测试准确率 \textbf{88.53\%}，作为基线。
    \item \textbf{DeepFool 攻击：} 局部线性化、投影到最近决策边界，求\textbf{最小 $L_2$ 扰动}；
          实测平均 $L_2$ 仅 \textbf{0.16}，约 FGSM 的 $1/11$，骗过率却相当。
    \item \textbf{对抗训练：} 用 PGD（非 DeepFool）做 min-max 训练得到鲁棒模型；
          诚实评估需 Clean/Robust 配对 + 强攻击（APGD）。
  \end{enumerate}
  \vspace{0.5em}
  \begin{block}{一句话}
    攻击（DeepFool）量出模型有多脆，防御（PGD 对抗训练）把它补强；
    评估必须\textbf{诚实}，否则“假鲁棒”一戳就破。
  \end{block}
\end{frame}

\begin{frame}{参考文献}
  \footnotesize
  \begin{thebibliography}{9}
    \bibitem{deepfool} S.-M.~Moosavi-Dezfooli, A.~Fawzi, P.~Frossard.
      \emph{DeepFool: a simple and accurate method to fool deep neural networks.} CVPR 2016.
    \bibitem{fgsm} I.~Goodfellow, J.~Shlens, C.~Szegedy.
      \emph{Explaining and Harnessing Adversarial Examples.} ICLR 2015.
    \bibitem{pgd} A.~Madry, A.~Makelov, L.~Schmidt, D.~Tsipras, A.~Vladu.
      \emph{Towards Deep Learning Models Resistant to Adversarial Attacks.} ICLR 2018.
    \bibitem{cw} N.~Carlini, D.~Wagner.
      \emph{Towards Evaluating the Robustness of Neural Networks.} IEEE S\&P 2017.
  \end{thebibliography}
\end{frame}

\begin{frame}[plain]
  \begin{center}
    {\Huge 谢谢！}\\[1em]
    {\large 欢迎提问与讨论}
  \end{center}
\end{frame}

\end{document}
